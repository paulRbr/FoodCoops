version: '3'

services:
  database:
    image: "postgres:12"
    env_file: .env
    volumes:
      - "/var/lib/postgresql/data"
  app:
    build: .
    env_file: .env
    ports:
      - "8069:8069"
    links:
      - "database:database"
    volumes:
      - "/home/app/.local/share/Odoo"
